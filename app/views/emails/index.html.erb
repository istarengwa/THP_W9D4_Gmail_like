<div class='container'>
    <div class='actions'>
      <%= form_with(url: emails_path,method: :post) do |form| %>
        <%= form.submit 'Recevoir un email' %>
      <% end %>
    </div>
    <div class='show-email'></div>
    <div class='email-list'>
      <% @emails.sort.reverse_each do |email| %>
        <p id="delete_email"> <%= render 'email', email: email, remote: true %> </p>
      <% end %>
    </div>
</div>
